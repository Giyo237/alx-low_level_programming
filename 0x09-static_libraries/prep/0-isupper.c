#include "main.h"
/**
 * _isupper - checks for uppercase letters
 * @c: the operant of the code
 * Return: either 1 or c if succesful otherwise return 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else

	return (0);
}
