#include <stdio.h>
/**
 * main - Entry point of the program.
 *
 * Return: Always 0.
 */
int main(void)
{
	int a = 0, b = 1, c;

printf("%d, %d", a, b);
while (b <= n)
{
c = a + b;
printf(", %d", c);
a = b;
b = c;
}
printf("\n");
}
int main(void)
{
	int number = 10000;

	fibonacciSequence(number);
	return (0);
}
	return (0);
}
