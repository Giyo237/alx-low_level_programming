#include "main.h"
/**
* set_string - function that sets pointer to char
* @s: the prt to the prt of a char
* @t: the prt to the char
* Return: 0 if succesful
*/
void set_string(char **s, char *to)
{
	*s = to;
}
