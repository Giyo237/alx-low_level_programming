#include <stdio.h>
/**
 * main - c program that prints numbers from 0 to 9
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;
	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
			return (0);
}
